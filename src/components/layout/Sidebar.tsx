/**
 * Legacy global sidebar — remplacée par ModuleSidebar (architecture apps Odoo-like).
 * Conservé vide pour éviter les imports cassés éventuels.
 */
export function Sidebar() {
    return null;
}
